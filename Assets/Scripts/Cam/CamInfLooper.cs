using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//TODO: This is a script for the camera infinite looping effect.
// Make a slider (0~1) to indicate the "progress" of the loop
// The camera will move forward (z+) when progress increases, such that we "zoom in", and vice versa
// When the progree reaces 0.5, use PortalCalcCamTransform.CalculateNewCameraTransform
public class CamInfLooper : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
